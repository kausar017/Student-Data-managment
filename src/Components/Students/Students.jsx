
import PropTypes from 'prop-types';

const Students = props => {
    return (
        <div>
            <h1>hello students</h1>
        </div>
    );
};

Students.propTypes = {
    
};

export default Students;