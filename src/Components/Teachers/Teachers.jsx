
import PropTypes from 'prop-types';

const Teachers = props => {
    return (
        <div>
            <h1>hello teachers</h1>
        </div>
    );
};

Teachers.propTypes = {
    
};

export default Teachers;